artifacts:
  - id: RQ-66-WATCHED
    type: system-req
    title: "The mutation survey named its own missing tests — four codegen decisions no oracle watches, one of them an inverted comparison"
    description: >
      RQ-65-MUTANTS classified four sampled mutants **UNTESTED**: the mutation
      changes emitted bytes and the entire named suite stays green. Each one is
      not merely a gap — it is a SPECIFICATION FOR AN ORACLE THAT DOES NOT
      EXIST, written by the compiler itself.

        R3-direct/COND/select_with_stack.rs:6774
          `I32Eq => Condition::EQ`  ->  `Condition::NE`
          An equality comparison's condition code INVERTED on the direct
          selector, and nothing notices. RQ-65-PARITY predicted this exact gap
          from the other direction; the survey confirmed it empirically.

        R2-ir_to_arm/DROPMOV/optimizer_bridge.rs:6705
          the result `Mov { rd: R1, .. }` DROPPED on the optimized path.

        R4-shared/REG/liveness.rs:7025
          `aapcs_dead_at_return = [R2, R3, R12, LR]` -> R2 replaced by R3, so
          R2 silently leaves the dead-at-return set. An ABI fact with no test.

        R5-startup/IMM/main.rs:9295
          the startup's data-copy count register changed r2 -> r3. Compounds
          the finding recorded in v0.65: FOUR OF FIVE named-suite oracles SEED
          R11 THEMSELVES rather than booting the shipped `Reset_Handler`, so
          the startup's register contract is assumed by the harness and
          therefore untestable by it BY CONSTRUCTION.

      THE DELIVERABLE IS FOUR ORACLES AND A RE-MEASUREMENT, in that order. Build
      the oracle, prove it KILLS its mutant (that is the red-first: the oracle
      must fail on the mutated tree and pass on the clean one), then RE-RUN the
      survey and publish the new rate. `mutants_untested` is a CI-pinned ratchet
      that must FALL from 4.

      REPORT THE SILENT-SUBSET NUMBER, not just the headline. v0.65's cold
      review established the distinction and it is the one that matters:
      "81 % caught" means CI GOES RED, which counts the compiler refusing, a
      panic, and a non-vacuity floor firing. Restricted to the 15 mutants that
      changed bytes SILENTLY, 4 survive — **27 %** — and an execution
      differential caught only 2. That is the number this artifact moves.

      A SMALLER IMPROVEMENT HONESTLY MEASURED BEATS A LARGER ONE ASSERTED. If an
      oracle turns out not to kill its mutant, say so and keep the mutant
      UNTESTED rather than weakening the oracle until it passes.
    status: implemented
    release: v0.66
    tags:
      - oracle
      - mutation
      - coverage
      - north-star
    links:
      - type: derives-from
        target: BR-001
    fields:
      req-type: functional
      priority: must
      verification-track: differential
      issue: "#1189"
      done-when: "manual: each of the four UNTESTED mutants either has an oracle PROVEN to kill it (red on the mutated tree, green on the clean one) or is recorded as still-unwatched with the reason; the SILENT-SUBSET rate is re-derived by a mechanical rule and published; mutants_untested falls in claims.yaml WHEN a genuine suite replay (CI, where a real `python` exists) reclassifies the four sites — recorded here as the one sub-criterion this PR does NOT close, with the reason, rather than closed by hand-editing the ledger"
      landed: >-
        PR (feat/watched-1189), one increment: three execution oracles in the
        new `watched-1189-oracle` CI job —
        `scripts/repro/cmn_residual_compare_1189_differential.py` (+ .wat),
        `i64_result_pair_1189_differential.py` (+ .wat),
        `self_contained_boot_sweep_1189_differential.py` — and one unit test,
        `liveness::tests::aapcs_dead_at_return_exemption_is_exactly_the_scratch_set_1189`
        (rides `test`); `mutation_survey.py` gains `is_loud_effect`, the FIRST
        code implementation of the v0.65 cold review's own silent-subset rule
        (`docs/reviews/v0.65-cold-review.md`: loud iff the mutant's own
        corpus-triage `changed[]` recorded >= 1 `newly-declined` or
        `compile-timeout` entry — a property of the mutant's effect, not of
        which layer/oracle caught it; `mutation_survey.py` on `main` had NO
        loud/silent classification at all before this). Re-run over the
        EXISTING v0.65 ledger (no oracle invocation — pure re-derivation) it
        reproduces the published 27 % (4 of 15) exactly, plus the review's own
        named alternate 25.0 % (4 of 16, with the one borderline record
        `arm_backend.rs:1006:8` counted silent) — both pinned, neither
        replacing the other; `docs/status/MUTATION_SURVEY.md` rewritten to
        report both rates with the full derivation and to state plainly that
        no 38-mutant re-sampling ran this release (see "why `mutants_untested`
        does not fall" in that doc);
        `claims.yaml` adds the silent-subset pins and
        `SYNTH-MUTATION-WATCHED-RQ66`, and moves the exact emulation floor
        (three new `# ci-checks: emulations` declarations; re-derived by
        oracle_wiring_check.py on the lane tree). `mutants_untested`
        value/baseline are UNCHANGED at 4 — the live derivation still equals
        the pin, so the ratchet is green, but it has not fallen. Issues filed
        from the boot sweep's first run: #1240, #1241.
      verified-by: >-
        IMPLEMENTED and INDEPENDENTLY RE-VERIFIED at a73d5ac2 (the salvage
        review re-ran all four transcripts by hand rather than trusting the
        recorded ones). RED-FIRST per oracle, the mutation applied in-tree
        (the exact `before`->`after` text recorded in
        `docs/status/mutation_survey.json`), tree restored and rebuilt
        green after each: (1) cmn residual — RED on the `I32Eq => EQ -> NE`
        mutant (160 wrong values, all five i32.eq exports on the
        --no-optimize leg, exit 1), GREEN clean (2,636/2,636 vectors, 41
        exports x 2 self-contained legs, exit 0); (2) i64 result pair — RED
        on the `mov r1, <hi>` deletion (400 wrong high halves across every
        optimized-leg export, exit 1), GREEN clean (882 agree + 148 pinned of
        1,030, exit 0); (3) liveness ABI test — FAILED on the
        `[R2,R3,R12,LR] -> [R3,R3,R12,LR]` mutant ("R2 is AAPCS-dead at pop
        {..., pc}: the pass must reuse it ... left: R8, right: R2"), passes
        clean; (4) boot sweep — RED on the EXACT recorded data-copy mutant
        (movw-only, register 2 -> 3, movt left at R2 — a broader
        both-instructions mutation tried first over-collapsed the failure
        mode into 48 boot-faults and 0 truncations, so the narrower, ledger-
        matching mutation was used for the reported numbers): 210 findings —
        22 boot-faults on the 11 modules whose copy is < 64 KiB, 176 wrong
        values on the 12 whose count is truncated (e.g.
        `mem757_low_const_copy:copy_peek(0)` -> 0x0, wasmtime 0x67) — all 23
        data-carrying modules the ledger names; both floors fire; exit 1;
        GREEN clean (337 compiled, 331 booted, 0 boot failures, 17,850
        vectors, RESULT: PASS). Re-measured directly through `oracle_run.py`
        (the mechanism the emulation floor is keyed to): cmn_residual 2,638
        emulations / 2,636 wasmtime_calls (floor 2,500), i64_result_pair
        1,032 / 1,030 (floor 1,000), boot_sweep 18,199 / 17,868 (floor
        17,500) — matching claims.yaml's comment; the emulation-floor total
        itself sums each script's DECLARED threshold (2,500+1,000+17,500),
        not these live counts. NO 38-mutant re-sampling ran (this
        host has no bare `python`, and `mutation_survey.py`'s suite replay
        scores any non-zero step exit as KILLED — a replay here would
        misclassify every mutant, not just these four, on an environment
        defect rather than an oracle observing wrongness — see
        MUTATION_SURVEY.md). What DID re-derive cleanly, with no such
        dependency: `mutation_survey.summarize()`'s `is_loud_effect` — the
        v0.65 cold review's OWN corpus-effect rule, mechanized for the first
        time (mutation_survey.py had no loud/silent classification before
        this) — re-run over the committed ledger reproduces the PUBLISHED
        27 % exactly: `killed_loud=6`, `silent_changed=15`,
        `silent_untested=4`, 26.7 % (rounds to 27 %), silent kills 2/7/2; the
        review's own named alternate (`arm_backend.rs:1006:8` counted silent)
        gives `killed_loud=5`, `silent_changed=16`, 25.0 %, silent kills
        2/8/2 — both pinned, neither replacing the other (an earlier pass of
        this PR wrongly treated 25.0 % as a correction to 27 % before finding
        the review's written rule; see MUTATION_SURVEY.md's correction note).
        `mutants_untested` stays 4/4 (ratchet green, unchanged
        — see done-when). `claim_check.py claims.yaml` exits 0;
        `oracle_wiring_check.py --list --exact-emulation-floor <the value
        pinned in claims.yaml's SYNTH-ORACLE-CHECK-FLOORS-910-CI, +21000 over
        its v0.65 figure>` reports its derived total matching that pin
        exactly (verified green on the lane tree — the live number is not
        restated here so this line cannot rot the next time the floor
        legitimately moves; see claims.yaml/ci.yml for the current value);
        cargo fmt/clippy/test clean.
