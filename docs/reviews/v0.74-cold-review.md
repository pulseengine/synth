# v0.74 cold review — "The gate that cannot tell a compiler from a shell script"

**Commit reviewed:** `d7c2f55c`
**Reviewed:** 2026-09-24, before the tag, on `main` at that commit — all eight
artifacts and every script, oracle, fixture and pin they shipped.
**Reviewers:** two, fresh-context, no inherited framing, in isolated worktrees —
one PROSE-TRUTH, one GATE-POTENCY. Both were briefed that "looks good" is a
FAILED review unless it shows what it checked, that anything unverifiable must
be marked UNVERIFIED, and (for the potency pass) that showing a gate PASSING is
not evidence: only a gate made to go RED has been exercised, and only if it red
for the RIGHT REASON.

| pass | verdicts |
|---|---|
| prose-truth | 22 VERIFIED · **5 FALSE** · 15 MISLEADING · 4 UNVERIFIED (of 46 statements) |
| gate-potency | **44 mutations** (4 positive controls + 40 attacks) · **11 CAUGHT · 29 MISSED** |

Release defect counts, for trend: v0.67 **4**, v0.68 **10**, v0.69 **14**,
v0.70 **21 + 15**, v0.71 **20 + 14 + 1 at the cut**, v0.72 **16 + 20**,
v0.73 **17 + 22**, v0.74 round 1 **49** (20 prose + 29 gate).

A SECOND ROUND is owed on these corrections, briefed to ATTACK them. v0.73 is
why that sentence is here: five of its ten round-2 FALSE statements were inside
round 1's own corrections.

---

## The finding that matters most

**This release's central thesis was tested against itself and held, twice —
and the instruments it repaired are still mostly blind.** 29 of 40 attacks got
through. The pattern is consistent and worth naming in one place:

> Every fix this release shipped closed the SPELLING the previous reviewer
> used, not the CLASS. STUBPROOF now rejects a one-byte file and an ELF of the
> wrong arch — and accepts a recorded ELF replayed forever. APTBLIND now
> catches `apt` and one level of `bash scripts/x.sh` — and misses `aptitude`,
> `dpkg`, two levels, `./scripts/x.sh`, `.github/x.sh`, a python wrapper, a
> make wrapper, a heredoc and `uses:`. GATEOFFLINE extracted `fidelity_exit()`
> *because* the ritual consumes its exit code — and tested the function while
> leaving the call site untested, which is exactly what v0.73 did when it
> extracted `decide()` out of `gate()`.

That is not an argument for more gates. It is the measurement this release
exists to produce, and it is recorded here rather than fixed in the commit
before a tag — which is how v0.72's second silent miscompile arrived, inside
the fix for the first.

## Corrected before the tag

**FALSE — a docstring refuted by the commit that wrote it.**
`gen_islandpass_1331.py` said the generator "was NEVER COMMITTED —
`git log --all --diff-filter=A -- '*gen_islandpass*'` matches nothing, on any
ref, ever." At HEAD that command returns `bd365555`: this file's own creation.
Dated rather than deleted.

**FALSE — `.text 6648`.** Measured 6656 for `islandpass_1331_backspan.wat`
(6636 for nested is correct), on every target probed. The figure was taken
before the same lane's odd-multiplier and trip-mask edits to the fixture it
counts — a number invalidated by the edit that writes it, for the third
release running.

**FALSE — "odd factors give 5".** Per fixture: 5 on `nested`, **4** on
`backspan` (args 0 and 1 collide under the masked trip count), **2** on the
ungenerated `spanning`. An argument-dropping miscompile mapping 0→1 is
invisible on two of the three.

**FALSE — the `carried-from` counts, and my own v0.73 correction was the
second wrong answer.** v0.72 wrote 44, measured before the commit that added
the 45th. v0.73 "corrected" it to 45 using `git grep -c`, **which counts LINES
CONTAINING A MATCH, not occurrences** — the true figure at that tag was 46.
At HEAD it is 55 occurrences across 23 files, structured on 19 artifacts. The
method is now named in the comment, because two releases got it wrong two
different ways.

**FALSE — "all four tests asserted `instructions[1].is_dead`".**
`test_algebraic_multiple` asserts `[2]` and `[3]`. The substance holds: all
four encoded the unsound transform as the expected outcome.

**MISLEADING, corrected:** `ORACLE_WIRING.md`'s heading and table still said
*eight* manual scripts 43 lines below the pin that moved to nine — in the same
commit whose own comment says "the prose cannot lag the budget it describes".
`_release.yaml` carried one statistic in a file whose header says it carries
none. STALEMSG's partition did not sum (105+9≠117) because the third bucket was
omitted. `gen_islandpass`'s "bytes" are characters. The census's "not pinned
anywhere" implied re-running replaces figures restated by hand in four other
files. `#1223 CLOSED` named an issue that is OPEN — the PIN is closed; closure
here is release-based.

**MISLEADING, and the sharpest of them: the #345 pin named a dead branch.**
The reason given for not rewording the message was that
`islandpass_1331_execution_differential.py` pins its text as `PINNED_REFUSAL` —
but that constant is consulted only inside STATE 1, reached when a fixture
fails to compile, and all three now compile. By the fixed point's own success
that branch cannot run. The text *is* pinned, by `litpool_islands_345.py`'s
`REFUSAL` regex and the byte-identity differential, both on the islands-OFF
leg. Right conclusion, wrong mechanism.

## Repaired gates, each proven by mutation

**The silent skip (`islandpass_1331_execution_differential.py`).** A fixture
reverting from "compiles and executes bit-exact" to STATE-1 "refused" scored
**green**: STATE 1 returns 0, prints no `RESULT:` line, and the declared floor
counted subprocess INVOCATIONS, which a refusing compiler still makes.
Demonstrated with a wrapper disabling islands for `nested` only — rc=0, two
PASS lines, and the CI grep satisfied. The floor is now
`emulations >= 15` (3 fixtures × 5 args). Re-run under the same wrapper:
**rc=1, `VACUOUS — declared floor emulations >= 15, measured 10`**, while the
script itself still exits 0. That is why a floor counts work done, not calls
made.

**INV2 was documented and never implemented.** `island_invariants`' docstring
claimed "every LDR.W literal resolves to a relocated word". `reloc_offsets` is
built and never read — two lines, an assignment and an `.add`, no use. A gate's
docstring asserting a check it does not perform is this release's subject
pointed inward. Corrected to say so, and to name what partially covers the
intent (`execute()`'s `nrel >= 3`).

**Arm D (`1 * x`) had no coverage anywhere.** Reverting it to `is_dead`, AND
emitting the Copy from the constant instead of from `x`, both survived
`cargo test --workspace` (168 suites, 3144 passed) and the execution
differential. `test_algebraic_mul_one` covers only `x * 1`; the commutation was
never tested, so a silent wrong answer was free. `test_algebraic_one_mul` now
exists and **both mutations fail it**.

## The measurement that disagreed with itself

PINDEBT2 claimed "only 2 of the 5 arms are observably wrong". The reviewer
rebuilt the pre-fix compiler independently, ran its own five probes, and
measured **4 of 5**. Two measurements of one quantity disagreed — and **neither
was reproducible**, because both probe sets lived in scratchpads, in a
repository whose stated rule is "derive what you check against from the
artifact you ship".

The probes now ship: `scripts/repro/algebraic_identity_probes_1223.py`, wired,
executing each shape against wasmtime through the home-alias harness. Re-run on
a pre-fix build they give **2 of 5**; post-fix **0 of 5**. The reviewer's shapes
are not in the tree and their figure is not reproducible here.

**The correction is not the digit — it is the claim's scope.** Masking is a
property of the chosen probe, not of the arm. `x - 0` and `1 * x` are unsound by
the same construction whether or not a given shape catches them, which is why
all five were repaired and not only the two these shapes expose. "2 of 5" is
true of THESE FIVE SHAPES and nothing wider.

## Recorded, not fixed — each with its mutation

These are v0.75 candidates. Six gate changes in the commit before a tag is how
v0.72's second miscompile arrived.

- **STUBPROOF is defeated by replay.** A 30-line script copying three
  pre-recorded ELFs — right machine, right symbols, `.text` overwritten with
  `0xDE` filler — passes the whole step: rc=0, `CHECKS=12/12`, `RESULT: PASS`,
  both CI greps. `inspect_object` asks what the file IS, never what it contains
  or who produced it. Separately, the two invocations are never cross-checked:
  it asserts `compiled ⊆ symbols` and never `declined ∩ symbols = ∅`, so an
  object whose symtab holds the three exports leg 1 just declined passes. And
  the floor move 3 → 6 bought nothing, because `mode=compiles` counts
  invocations.
- **`merge_gate`: 10 of 10 mutations to `gate()`/`main()` pass `--self-test`**,
  including forcing the whole rollup to SUCCESS and `ok &= True`. `--self-test`
  reaches `decide()` and `fidelity_exit()` and nothing else.
- **`ci_pool`: nine enumerated walk-arounds** — `aptitude`, `dpkg`, two script
  levels, `./scripts/x.sh`, `.github/x.sh`, a python wrapper, a make wrapper, a
  heredoc, and `uses:`. One `uses:` mutation WAS caught, but only because that
  action's *name* contains "apt". The population is latent today: no required
  context sits on the self-hosted pool.
- **`FIELD_KEYS` shrinkage is unguarded** — deleting an entry from
  `FIELD_KEY_CONSUMERS`, or emptying `FIELD_KEYS_UNREAD`, narrows R13 silently.
  The same "cannot see population shrinkage" defect FIELDSHAPE names one file
  over, in its own fix.
- **A spanning fixture can be shrunk below the island threshold undetected.**
  Deleting 900 lines of padding from `islandpass_1331_spanning.wat` drops
  `.text` to 2,988 bytes — under the 4,095 reach, so no island is placed — and
  the differential still reports "compiles AND executes bit-exact". Nothing
  asserts a spanning fixture still needs an island.
- **The census's stated population is short by 54 sites.** The
  `#[cfg(test)]`-to-EOF cut discards lines 308–19797 of `instruction_selector.rs`
  because the first marker decorates a test-only helper while `mod tests` is at
  9205. And the ARM-immediate lookbehind excludes ZERO sites from the shipped
  population — it is correct but inert. **The conclusion survives every
  alternative definition tried** (93/93/93/94% by sites, 90% by numbers), which
  is why the verdict stands and only the population is corrected.

## What neither reviewer could settle

The home-alias corpus pins (`7263 → 7275`, `8328 → 8332`, `229537 → 235249`)
could not be re-derived: the spec-testsuite submodule is not checked out in a
worktree, so the sweep aborts with "found 0 top-level .wast, want 257". What did
run matched `ci.yml`'s pin verbatim. The branch-retarget mutation results quoted
in ISLANDREACH's own docstring were re-derived by the gate reviewer and hold;
the prose reviewer marked them UNVERIFIED because it did not mutate the
compiler. The artifact and its commit describe the same mutated run differently
("UNICORN FAULT ×3" vs "1/5 args bit-exact") — not contradictory, but neither is
re-derivable from the tree alone.

---

# Round 2 — briefed to ATTACK round 1's corrections

**Commit reviewed (round 2):** `a1dc2fa5` — the tree WITH round 1's
corrections. (The reviewers ran on the pre-squash branch head, whose tree is
byte-identical to this merged commit; the branch sha is deliberately NOT
written here, because conformance step 7 reads every hex run in this file as
a candidate reviewed-commit and a non-ancestor is noise in a gate's input.)
Two fresh-context reviewers, isolated worktrees, no inherited framing.

| pass | verdicts |
|---|---|
| prose-truth | 9 VERIFIED · **6 FALSE** · 2 DEFECT · 6 MISLEADING · 1 UNVERIFIED (24 items) |
| gate-potency | **54 mutations · 21 CAUGHT · 33 MISSED** |

**Every one of the six FALSE statements, and both defects, are inside round 1's
own corrections or the files it edited.** That is the v0.73 pattern reproducing
exactly, and it is the reason this round exists.

## The finding that changed a shipped gate

**The probe oracle round 1 shipped was 2/5 potent for its own stated
population.** Every shape was wrapped in `(i32.add <shape> (local.get 0))`, and
that wrapper masks three of the five arms it names. Demonstrated: with `x - 0`
reverted to the defect, the wrapped shape returns wasmtime's 14 — clean — while
the bare shape returns 0 against wasmtime's 7.

Worse, that wrapper is why round 1 got the underlying disagreement backwards.
Round 1 measured "2 of 5 wrong pre-fix", a reviewer measured 4 of 5, and round 1
**adjudicated in favour of its own weaker probes** and recorded the other figure
as unreproducible. Bare shapes reproduce it exactly.

Fixed: the shapes are bare. Re-measured by reverting each arm INDIVIDUALLY, the
oracle now reds on **4 of 5** (A, B, C, E) where it red on 2. Arm D (`1 * x`) is
still masked at this shape and is covered instead by `test_algebraic_one_mul`.
No arm is covered by two mechanisms, and that is stated rather than implied.

**A second defect in the same file:** the harness signature was
`("i32", ["i32"])` — the convention inverted. `_pack_args32` compares each width
to 32, so every argument took the 64-bit PAIR branch and clobbered R1's canary,
the seed that exists to make an undefined-register read visible. Corrected to
`([32], 32)`.

## Arm A had no unit test, and round 1 walked past it

Round 1 found the MUL commutation gap, wrote `test_algebraic_one_mul`, and left
the identical ADD commutation one arm away. `test_algebraic_add_zero` builds
`Add { src1: Reg(1), src2: Reg(0) }` — that is `x + 0`, arm B. The commuted
`0 + x` is never built, and **arm A is the one #1223 was actually reported
against**. Reverting it passed `cargo test --workspace` in full. Round 1's own
new comment on that test claimed it covered "`(x - x) + x` returning 0", which
is arm A — a false sentence written while fixing a false sentence.
`test_algebraic_zero_add` now exists and fails on both mutations.

## A round-2 finding this release REFUTES

Round 2 reported `ci.yml`'s new step as "a required-job step that cannot run
under its own interpreter (rc=127)". **Refuted.** Bare `python` is absent
LOCALLY, not on the self-hosted runners: this exact step ran on PR #1388 and
printed `algebraic-identity-probes: 5 shape(s) executed / silent wrong answers:
0 of 5`, and 18 self-hosted steps use bare `python` today with their jobs green.
The step was changed to `python3` for consistency with its nine siblings, not as
a repair. Recorded because a reviewer finding that does not hold is exactly as
important as one that does.

## Round 1's other false statements, corrected

- **"v0.72 wrote 44 … two releases got it wrong two different ways."** `git show
  v0.72.0:scripts/status_evidence_check.py` has no such sentence; the 44 entered
  in `03d87d05`, a v0.73 lane. Per-commit occurrence counts from v0.72.0 read
  42, 45, 45, 45, 45, 45, 46 — never 44. And 44 and 45 come from the SAME
  line-counting method. The corrected figure (55/23/19) stands; its story did not.
- **"invisible on backspan and on spanning."** `spanning(0)` and `spanning(1)`
  differ, so a 0→1 miscompile IS visible there. Invisible on exactly one fixture.
- **"even factors gave 1 distinct result."** True of backspan; nested still gives
  5, because its `br_if 1` skips the chain for every non-zero argument — a
  per-fixture number generalised in the paragraph that says not to.
- **"`--self-test` reaches `decide()` and `fidelity_exit()` and nothing else."**
  It also drives `branch_currency`, `squash_fidelity` and `is_advisory`. What is
  true, and was the point: it does not reach `gate()` or `main()`.
- **"PINNED, in three places."** Exactly one — `litpool_islands_345.py`'s regex,
  which spells out `(#345)`. The differential's constant is the prefix only; the
  execution differential's sits on an unreachable branch. Round 1 corrected the
  first wrong mechanism and introduced a second.
- **A third copy of the manual budget** still said `count-max 7`, 148 lines below
  the pin round 1 moved to 9.

## Recorded, not fixed — with mutations, as v0.75 candidates

- **The islandpass floor is bound to 3×5, not to the work.** Adding a fourth
  fixture entry restores the silent skip: the wrapper that disabled islands for
  one fixture then passes at `measured=15`. Nothing derives the floor from
  `len(FIXTURES) * len(ARGVALS)`.
- **The same oracle can be made to compare the compiler with itself.** Replacing
  the wasmtime reference with `run(arg)` yields rc=0, `wasmtime_calls=0`, a
  printed "5/5 args bit-exact vs wasmtime", and a floor that RISES to 30.
  `wasmtime_calls` is printed by two ledgers and gated by neither.
- **`ARGVALS` can be set to all zeros** with output byte-identical to baseline.
- **`APT_RE` only ever sees an installer that is the FIRST TOKEN of its `run:`
  value.** `str(job)` is a dict repr, so newlines render as the characters `\`
  and `n`, and `\bapt\b` then sees a word character in front. Same two lines
  swapped: `apt-get` first → CAUGHT; `set -euo pipefail` first → MISSED. That
  subsumes the "heredoc" entry and is a deeper mechanism than the nine spellings
  round 1 enumerated. Plus three more walk-arounds: `sh -e scripts/x.sh`, a
  runner `group:` (which `pool_of` never classifies as self-hosted, skipping the
  guard entirely), and a quoted script path.
- **Three of `merge_gate`'s 25 checks do not cover what they appear to.**
  Hardwiring CHECK2's `current` to True, dropping `ERROR` from CHECK3's set, and
  dropping the advisory filter from CHECK3b all leave `--self-test` green.
- **The emulation-floor equality can be defeated by offsetting** — one oracle's
  floor cut while another's rises by the same amount. One scalar over 173
  scripts cannot localise.
- **The probes oracle probes each shape at exactly ONE input (`7`)**, so a stub
  returning a per-shape constant passes.

## What round 2 could not settle

Round 1's own tally (22/5/15/4 of 46) is internally consistent but its
statements were never enumerated, so it is not checkable. The home-alias corpus
pins remain unverifiable in a worktree (submodule absent), though `7275`, `8332`
and `235249` do appear verbatim in `ci.yml`. The census's "94%" variant depends
on an axis the text never defines.
