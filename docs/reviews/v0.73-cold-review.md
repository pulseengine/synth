# v0.73 cold review — "Is it still broken?"

**Commit reviewed:** `bcdbab11`
**Reviewed:** 2026-09-24, before the tag, on `main` at that commit — the four
lanes merged at that point (PINDEBT, FALCONFIXTURE, ISLANDPASS, SC5+STEP8) plus
every script, oracle and artifact they shipped.
**Reviewers:** two, fresh-context, no inherited framing — one PROSE-TRUTH, one
GATE-POTENCY. Both were briefed that "looks good" is a FAILED review unless it
shows what it checked, that anything unverifiable must be marked UNVERIFIED,
and (for the potency pass) that showing a gate PASSING is not evidence: only a
gate made to go RED has been exercised.

| pass | verdicts |
|---|---|
| prose-truth | 94 VERIFIED · **9 FALSE** · 10 MISLEADING · 10 UNVERIFIED (of 123 statements) |
| gate-potency | 6 gates exercised, 49 mutation runs, **8 findings** across 5 gates |

Release defect counts, for trend: v0.67 **4**, v0.68 **10**, v0.69 **14**,
v0.70 **21 + 15**, v0.71 **20 + 14 + 1 at the cut**, v0.72 **16 + 20**,
v0.73 round 1 **17** (9 prose + 8 gate, of which 4 gate findings were already
fixed on the unmerged GATETRUTH branch — see below).

A SECOND ROUND is owed on the lanes that merge after this review (GATETRUTH,
CIPOOL, ARCHMODEL) and on these corrections themselves, and must be briefed to
ATTACK them. v0.72 is why that sentence is here: its round 2 found 17 more
false statements and broke three of round 1's own fixes, including a silent
miscompile introduced inside the fix for a silent miscompile.

---

## What the potency pass could and could not see

The reviewer ran against `main` at `bcdbab11`, which PREDATES RQ-73-GATETRUTH.
Four of its eight findings (CHECK1 comparing cardinality, the census pin's
tautology, `FIELD_KEYS` having no derivation, `REQUIRED_CONTEXTS = []` passing
silently) name defects that lane already fixes, and the reviewer said so itself
after finding a worktree on that branch. They are counted above but are not new
work.

The other four are new and appear in no artifact.

## Gate findings, and the fix for each

### F1 — a typo'd issue reference was silently absorbed

`stale_pin_check.py` booked any value with no exact `^#\d+$` match as
"reason-charged". The `attributed + reasoned == declared_total` invariant —
built precisely to prove no pin is skipped — still balanced, so the gate printed
PASS while never checking that issue for closure.

Reproduced: `('#1206', 2)` → `('issue #1206', 2)` in the parity table drops
#1206 from the checked set (8 issues → 7, attributed 68 → 67, reasoned 17 → 18,
total still 85).

The anchored regex is RIGHT and stays — `#539-grow-fails` must not read as issue
539. What was missing is that the reason bucket was a catch-all. It is now a
DECLARED set of exactly two strings, enumerated from the live tables, and any
other value carrying a `#N` is refused by name.

### F2 — a null stub passed the falcon oracle's entire CI step

`falcon_opt_1318_differential.py` inferred "compiles" from the ABSENCE of a
`warning: skipping` line, never read the return code, and wrote the object to
`/dev/null`. A 14-line shell stub printing the six expected warnings and exiting
3 satisfied the whole step — `oracle_run.py` floor included, because
`compiles >= 3` counts INVOCATIONS, not successful compilations.

The six cells at risk were the CONTROLS: the i32 and AArch64 cells that prove
the oracle still discriminates. The exit status must now agree with whether
anything was skipped, and a clean compile must produce a non-empty object.

### F3 — the verdict that detects #1269 ask 2 was never exercised

Both `squash_fidelity` calls in `--self-test` passed `(x, x, base)`, so
`diff_lines` was always 0 and the `SQUASH ALTERED CONTENT` branch was
unreachable from the fixture. Replacing `if diff_lines == 0:` with `if True:` —
deleting the detection outright — left the self-test green.

This is the live path: `merge_ritual.sh` calls it after every merge. A
current-branch-with-nonzero-diff case now exists, and fails on that mutation.

### F8 — the deadlock guard never asked whether the target pool can run the job

Its scope is "defined / has `runs-on` / no `if:`" — all properties of the job's
DECLARATION. Moving a required context to self-hosted produced ZERO deadlock
failures; it red only on the RATCHET, and lowering the ceiling exactly as the
ratchet instructs gave "0 failure(s)".

**And the artifact's stated reason was wrong for half the set.** RQ-73-CIPOOL
says the four required contexts "can NEVER move" because `no_new_privs` forbids
`apt-get`. Measured on this tree, only TWO need apt (`Test`, `Z3 Verification`);
`Kani Verification` and `Bazel Build & Proofs` do not, and five required
contexts already run self-hosted today. The true statement is narrower — a
required context that needs apt can never run self-hosted — and that is what the
new check enforces. The general claim is false and would eventually license
"working around" a blocker that was never there.

---

## Prose findings

Nine FALSE. Three are worth naming because of WHERE they came from.

**A sibling lane invalidated an anchor and fixed only the machine-readable
copy.** RQ-73-FALCONFIXTURE cited `arm_backend.rs:1061`. RQ-73-ISLANDPASS, in
this same release, shifted every line in that file by −22 and re-anchored five
`mutation_survey.json` ids FOR EXACTLY THAT DRIFT — while leaving the prose
pointing at the dead line (the match is at 1039). The ledger was treated as
load-bearing and the sentence was not.

**A quotation the compiler has never been able to emit.** Four places quoted the
SC-5 gate as reporting "…which is not an instruction start". It says "not an
instruction BOUNDARY", and `git log -S "not an instruction start" --
crates/synth-backend/src/arm_backend.rs` returns NO commit, ever. The phrasing
quoted was this release's own NEW oracle's, attributed to the compiler as if
observed. No green CI can catch that class.

**A number that disagreed with the record it cited as agreeing.**
"fused.wasm rc=0, 0 of 26 skipped (still clean, as at v0.70.0)" — the module has
21 exported functions and v0.70's own record says "1 of 21 skipped". The 26 came
from counting ELF `FUNC` symbols, which include helpers.

The rest: `island_offset_invariant_345.py` "304 objects" where the SAME FILE
says 306 eleven lines earlier (a plan-era figure never re-measured, sitting in
one clause beside 165/0 MOVED, which IS the post-lane number); "28 UNPINNED
open issues" reusing the untouched-since-filed figure for a different set (33);
"Several of the suppressed issues are titled SILENT MISCOMPILE" when ZERO of
the eight titles contain that phrase; "nearly tripled" for 27 → 85 (3.15×, more
than); and a title asserting "#1318 cannot be measured in this repository" that
the artifact's own body refutes.

Every correction re-derived the value here rather than taking the reviewer's —
the correction is exactly where the next false statement gets introduced.

## What the reviewers could not settle

Ten UNVERIFIED, honestly marked rather than guessed: the `state_reason` at the
moment #1229 closed (the timeline returns it empty and the live value is now
REOPENED); "28 of 40 untouched since filed" (`updatedAt` is a moving field, and
four v0.73 lanes touched board issues after the measurement); the states of
unshipped drafts; and the v0.72 miscompile byte-evidence, whose builds no longer
exist.

## The measurement this release did not get

RQ-73-PINDEBT's deliverable (a) was "drive `known_open_pins` DOWN". It ends at
**85**, the value it began at. No pin was closed by any lane. The artifact ships
on deliverable (c) — the stale-pin tripwire — which its `verified-by` names
honestly, but the three-deliverable framing reads as three things done and one
of them was not.
