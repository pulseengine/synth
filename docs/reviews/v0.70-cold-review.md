# v0.70 cold review

Two passes, both recorded: a **pre-merge** pass on `feat/npa-1331 @ 4e865f31`
(21 findings, none of whose fixes landed before #1340 merged), and an **RC**
pass on the release candidate itself. Conformance step 7 requires the second;
the first is recorded because a review whose findings never land is the exact
failure this release is named after.

## Why pass 1 is in this file at all

The pre-merge review ran, produced 21 findings, and #1340 then merged with all
nine artifacts and **none** of the corrections. That is the v0.69 failure mode —
six false statements shipped — about to repeat in a release titled *"Evidence
you can re-derive"*. The corrections were applied on the release branch instead,
each one **independently re-derived before being acted on**, which is why two of
them ended as refutations rather than fixes.

## Pass 1 — dispositions

Every finding below was re-derived on the release tree before disposition. A
"CORRECTED" row means the original claim was confirmed wrong and the text now
states the measured value.

| # | finding | disposition |
|---|---|---|
| 1 | RIVETNOTES: `8 added`, `22 NEW WARNINGS in three classes`, `five artifacts use` | **CORRECTED** → 9 added; 26 warnings in four classes; six artifacts. Re-derived again at the cut (see pass 2, finding A). |
| 2 | CITEGAP: `135 files`, `30 of 135`, `ALL 8 of v0.70's`, `5233 -> 7224` | **CORRECTED** → 136 / 136 / ALL 9 / `5243 -> 7242` (the last re-derived at the cut). |
| 3 | NPA + `npa_static_f32_1331.wat`: "a 20-line reduction" | **CORRECTED** → 12 code lines, 41 total, 38 non-blank, stating the convention it shares with `spill_slot_alias_1321.wat`. |
| 4 | FALCONCORPUS: the same "20 lines"; a four-files claim naming two | **CORRECTED** → all four named; "one of v0.69's TWO `must` artifacts". |
| 5 | ALIAS: `compiled=174/194` — the `.wat` population is 195 | **CORRECTED** → `compiled=175/195`, re-measured on the release tree (`executed=3428/3428`, `mismatches=0`, PASS). |
| 6 | DONEWHEN table: `v0.61 11/15` | **CORRECTED** → 12/15. |
| 7 | WINDOWVAC: "the current v0.70 window (1 commit) is correctly silent" | **CORRECTED** → replaced with the derived line, `2 delivery-shaped commits since v0.69.0 — 2 attributed`. |
| 8 | `spill_slot_alias_1321.wat`: a false `EXPECTED_DECLINES` claim | **CORRECTED** → "RED-FIRST BY CONSTRUCTION, AND DELIBERATELY NOT PINNED … carries NO `EXPECTED_DECLINES` entry … and never did". |
| 9 | Three code comments stating the opposite of the code they head | **CORRECTED** → `instruction_selector.rs` and both `select_with_stack.rs` sites now describe relocation, and disclose the bounds-check bypass inline (finding 19). |
| 10 | `artifact_citation_check.py`: `except Exception: continue` — an unparseable artifact is silently skipped | **FIXED, red-first.** A gate that skips what it cannot parse cannot fail on it. Now hard-fails and names the files. |
| 11 | Same file used `yaml.safe_load` (last-wins on duplicate keys) while its sibling is strict | **FIXED** by importing the sibling's `StrictLoader`/`DuplicateKeyError` — no second definition to drift. Both reds reproduced before and after; the green path is unchanged. |
| 12 | R9 cannot reach 6 of v0.70's 7 "evaluable" signatures | **CORRECTED** → DONEWHEN's false causation replaced by a measured disclosure: R9 is disarmed by ANY non-empty `verified-by`, and `is_crate_source()` is `parts[0] == "crates"`, so a `contains:scripts/...` — 4 of 7 signatures — is outside its reach entirely. |
| 13 | Neither of the generator's refusals is pinned by a test | **FIXED** → `test_release_notes_from_rivet.py` 8 → 11 tests: both refusals driven against a fake rivet and a throwaway tagged repo, **plus a negative control** proving the reds are the refusals and not an unreachable harness. |
| 14 | `artifacts/release-v0.70/_release.yaml` does not exist | **RECORDED, NOT BACK-FILLED** — see below. |
| 15 | PAGELIB titled "DEFERRED" with no `disposition:` and no `verified-by` | **FIXED** → `disposition: deferred` and a `verified-by` block added. |
| 16 | Suspected soundness narrowing: the i64 area is unpoliced when `spill_area_reserved == false` | **REFUTED** — see below. |
| 17 | `arm_backend.rs` frame self-check matches ANY `Sub{SP,SP,Imm}` | **RECORDED, DEFERRED** — see below. |
| 18 | `policed`'s `Some([])` guard does not cover `Some(&[a..a])` | **FIXED, red-first.** |
| 19 | Undisclosed behaviour change: the f32 npa branches bypass the bounds-check helpers | **DISCLOSED** at both call sites, in RQ-70-NPA, in the CHANGELOG and in the PR body. |
| 20 | ALIAS's "R9 REFUSED that" — the rejection was the author's, not the gate's | **CORRECTED.** |
| 21 | Counts in NPA and ALIAS not re-derivable from this repository | **CORRECTED** → the reporter-supplied figures now carry an explicit `[REPORTER-ONLY]` label, and ALIAS's evidence header reads "Re-derivable from this repository EXCEPT where marked [REPORTER-ONLY]". |

### Finding 16 — REFUTED, and how

The claim: with `spill_area_reserved == false` and `spill_base` Some, the i64
area is not recorded, so a slot handed out at `i64_spill_base` — the region that
aliases the #204 param homes, the original #331 shape — would be **unpoliced**,
a class the validator caught before #1321.

The state is unreachable. All **eight** non-test `spill.alloc()` sites are
dominated by a guard:

- `instruction_selector.rs:8461` (arg-move cycles) and `:8650` (call-result
  parking) test `spill.area_reserved` directly and return the ladder-recoverable
  exhaustion `Err`.
- `:1076` is gated on `spill_on_exhaustion` by a **short-circuiting `||`**;
  `:3392` and `:5479` on `vfp_frame_home_locals`; `select_with_stack.rs:884` and
  `:4936` on `vfp_spill_on_exhaustion`. Every one of those rungs **implies** the
  area, because `select_with_stack.rs:296` passes exactly that three-way
  disjunction as `force_spill_area`, and `has_i64` ORs it in.
- `:1867` (`alloc_consecutive_pair`) is reached only by i64 pair allocation,
  which implies `has_i64` by the same derivation.

That last one rests on an invariant rather than a check, so it was **measured**:
an `assert!(self.area_reserved, …)` planted in `SpillState::alloc` fired **zero**
times across the 766-test `synth-synthesis` suite and the corpus sweep's 175
compiled modules / 3428 executed vectors. A `#[should_panic]` control in the same
build confirmed the assert **does** fire when `alloc` is reached unreserved — so
the zero is evidence, not a dead probe. The probe was reverted and the source
sha256 restored byte-identical; it is not in the shipped tree.

### Finding 14 — recorded, deliberately not back-filled

v0.70 is the **first release since v0.61 with no `_release.yaml`**; the plan PR
#1336 omitted it, which is why v0.69 ships 10 files under its release directory
and v0.70 ships 9.

It is **not** created here. The file is a *plan-time* document — v0.69's reads
"EVERY ARTIFACT HERE WAS VERIFIED AGAINST SOURCE ON v0.68.0 **BEFORE IT WAS
WRITTEN**" and closes with "HONEST RISKS, recorded before the work". Authoring
that at the tag manufactures a plan-time record, in a release about evidence you
can re-derive. No gate reads it either: `status_evidence_check.py`'s R0 constrains
the file only **if present** (it must parse, and must be comments-only, because a
keyed `_release.yaml` is skipped silently by rivet). Its absence is invisible.

So the gap is named here with its date and cause, and **v0.71's planning PR
creates `artifacts/release-v0.71/_release.yaml`** — at plan time, where it belongs.

### Finding 17 — recorded, deferred, with the measurement

`arm_backend.rs:2219+` asserts the frame is unchanged with
`arm_instrs.iter().any(|ins| Sub{SP,SP,Imm(n)} if n == frame_size)` — **any** such
instruction, not the prologue's. It is safe today: the only `rd: Reg::SP` `Sub` in
`select_with_stack.rs` is line 330, and every other SP write is an `Add`. Equal
`frame_size` implies unchanged areas only because both derive from the same
`layout`. That is a true statement about today's emitter, not a structural tie, and
a second SP-decrementing site would make the self-check pass vacuously. Deferred
rather than changed at a release cut, because tightening a self-check on the
shipped codegen path needs its own oracle and its own red-first case.

