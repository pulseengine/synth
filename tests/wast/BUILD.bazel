load("//build:wast_test.bzl", "wast_multi_func_test")

# =============================================================================
# WAST-driven integration tests for Synth
#
# Each .wast file contains a (module ...) definition plus (assert_return ...)
# test cases. The wast_multi_func_test macro:
#   1. Compiles the module to ARM ELF with --all-exports
#   2. Generates a Robot Framework test from the WAST, reading function
#      addresses from the ELF symbol table
#   3. Runs the tests on the Renode Cortex-M4 emulator
# =============================================================================

# --- i32 arithmetic (add, sub, mul, and, or, xor) ---

wast_multi_func_test(
    name = "wast_i32_arithmetic",
    wast = "i32_arithmetic.wast",
    tags = ["i32", "arithmetic"],
)

# --- i32 comparison (eq, ne, lt_s, lt_u, gt_s, gt_u, eqz) ---

wast_multi_func_test(
    name = "wast_i32_compare",
    wast = "i32_compare.wast",
    tags = ["i32", "comparison"],
)

# --- i32 simple equality (single-function module) ---

wast_multi_func_test(
    name = "wast_i32_eq_simple",
    wast = "i32_eq_simple.wast",
    tags = ["i32", "comparison"],
)

# --- i32 division (div_s, div_u) ---

wast_multi_func_test(
    name = "wast_i32_div",
    wast = "i32_div.wast",
    tags = ["i32", "division"],
)

# --- i32 remainder (rem_s, rem_u) ---

wast_multi_func_test(
    name = "wast_i32_rem",
    wast = "i32_rem.wast",
    tags = ["i32", "remainder"],
)

# --- i32 bit count (clz, ctz, popcnt) ---

wast_multi_func_test(
    name = "wast_i32_bitcount",
    wast = "i32_bitcount.wast",
    tags = ["i32", "bitcount"],
)

# --- i32 shift (shl, shr_s, shr_u) ---

wast_multi_func_test(
    name = "wast_i32_shift",
    wast = "i32_shift.wast",
    tags = ["i32", "shift"],
)

# --- i32 rotate (rotl, rotr) ---

wast_multi_func_test(
    name = "wast_i32_rotate",
    wast = "i32_rotate.wast",
    tags = ["i32", "rotate"],
)

# --- i32 memory (store_load) ---

wast_multi_func_test(
    name = "wast_i32_memory",
    wast = "i32_memory.wast",
    tags = ["i32", "memory"],
)

# --- i64 arithmetic (add, sub, and, or, xor) ---

wast_multi_func_test(
    name = "wast_i64_arithmetic",
    wast = "i64_arithmetic.wast",
    tags = ["i64", "arithmetic"],
)

# --- i64 comparison (eq, ne, lt_s, gt_s, eqz) ---

wast_multi_func_test(
    name = "wast_i64_compare",
    wast = "i64_compare.wast",
    tags = ["i64", "comparison"],
)

# --- i64 multiplication ---

wast_multi_func_test(
    name = "wast_i64_mul",
    wast = "i64_mul.wast",
    tags = ["i64", "multiplication"],
)

# --- i64 division and remainder (div_u, div_s, rem_u, rem_s) ---

wast_multi_func_test(
    name = "wast_i64_div",
    wast = "i64_div.wast",
    tags = ["i64", "division"],
    timeout = "moderate",
)

# --- i64 shift (shl, shr_s, shr_u) ---

wast_multi_func_test(
    name = "wast_i64_shift",
    wast = "i64_shift.wast",
    tags = ["i64", "shift"],
)

# --- Control flow: select (ternary operator) ---

wast_multi_func_test(
    name = "wast_control_select",
    wast = "control_select.wast",
    tags = ["control", "select"],
)

# --- Control flow: if-else blocks ---

wast_multi_func_test(
    name = "wast_control_if",
    wast = "control_if.wast",
    tags = ["control", "if"],
)

# --- Control flow: loop with backward branches ---

wast_multi_func_test(
    name = "wast_control_loop",
    wast = "control_loop.wast",
    tags = ["control", "loop"],
)

# --- Control flow: nested if-else ---

wast_multi_func_test(
    name = "wast_control_nested_select",
    wast = "control_nested_select.wast",
    tags = ["control", "nested"],
)

# --- Control flow: br_if block pattern ---

wast_multi_func_test(
    name = "wast_control_br_if_select",
    wast = "control_br_if_select.wast",
    tags = ["control", "br_if"],
)

# --- Control flow: loop with select (conditional accumulation) ---

wast_multi_func_test(
    name = "wast_control_loop_if",
    wast = "control_loop_if.wast",
    tags = ["control", "loop", "select"],
)

# --- Control flow: nested loops ---

wast_multi_func_test(
    name = "wast_control_nested_loop",
    wast = "control_nested_loop.wast",
    tags = ["control", "nested", "loop"],
)

# --- Control flow: factorial (loop with multiply) ---

wast_multi_func_test(
    name = "wast_control_factorial",
    wast = "control_factorial.wast",
    tags = ["control", "loop", "factorial"],
)

# =============================================================================
# Example application tests
# =============================================================================

# --- Calculator example (arithmetic, bitwise, accumulator, comparison) ---

wast_multi_func_test(
    name = "calculator",
    wast = "calculator.wast",
    tags = ["example", "calculator"],
)

# --- Anti-pinch controller example (jam detection, PWM ramping, safety) ---

wast_multi_func_test(
    name = "anti_pinch",
    wast = "anti_pinch.wast",
    tags = ["example", "anti-pinch", "safety"],
)

# =============================================================================
# Official WebAssembly Spec Tests
# =============================================================================

wast_multi_func_test(
    name = "spec_i32_test",
    wast = "//tests/spec-testsuite:i32.wast",
    tags = ["spec", "i32"],
)

wast_multi_func_test(
    name = "spec_i64_test",
    wast = "//tests/spec-testsuite:i64.wast",
    tags = ["spec", "i64"],
)

wast_multi_func_test(
    name = "spec_select_test",
    wast = "//tests/spec-testsuite:select.wast",
    tags = ["spec", "select"],
)
